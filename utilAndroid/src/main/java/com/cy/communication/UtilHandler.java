package com.cy.communication;

import android.os.Handler;

public class UtilHandler {
    private static final String TAG = "UtilHandler";
    
    /**mainLooper的用法
     * @param perc
     */
    private void updatePercentageInLoadingDialog(int perc)
    {
/*        final int percent = perc;
        Handler mainHandler = new Handler(mainActivity.getMainLooper());
        Runnable myRunnable = new Runnable(){
            @Override
            public void run() {
                iUICallBack.setLoadingPercentage(percent);
            }};
        mainHandler.post(myRunnable);*/
    }
}
